
server:
  port: 8123

spring:
  application:
    name: ai-learning-agent
  ai:
    dashscope:
      chat:
        options:
          model: qwen-plus
      api-key: 你的key

#    mcp:
#      client:
#        sse:
#          connections:
#            server1:
#              url: http://localhost:8127
    mcp:
      client:
        stdio:
          servers-configuration: classpath:mcp-server.json
  profiles:
    active: local

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.cheems.ailearningagent.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn

